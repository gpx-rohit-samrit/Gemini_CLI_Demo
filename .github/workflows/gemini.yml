name: Gemini CLI Review

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  gemini-review:
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write   # Needed to comment on PRs
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Node.js (for Gemini CLI)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Gemini CLI
        run: npm install -g @google/gemini-cli

      - name: Run Gemini CLI (Java folder only)
        run: |
          gemini --include-directories ./src/main/java --yolo \
            -p "Analyze this Vert.x Java project for bugs, best practices, and improvements. Suggest specific changes with reasoning."
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Post Gemini Review to PR
        if: github.event_name == 'pull_request'
        run: |
          gemini --include-directories ./src/main/java --yolo \
            -p "Review the Java code for bugs, best practices, and improvements. Suggest changes with reasoning." \
            > gemini-review.txt

          gh pr comment ${{ github.event.pull_request.number }} --body-file=gemini-review.txt
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
